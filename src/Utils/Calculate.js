const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

const divideTheFirstNumByTheSecondNum = (first, second) => {
    return first / second;
}

// export default add;
// export default multiply;

export {add, multiply, divideTheFirstNumByTheSecondNum};

// const calculator = { Add: add, Multiply: multiply}
// export default calculator;